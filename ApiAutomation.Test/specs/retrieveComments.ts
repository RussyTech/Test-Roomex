import * as supertest from 'supertest'

const request = supertest('https://jsonplaceholder.typicode.com/');

describe('retrieve comments from post and confirm response includes email address', ()=>{
    it('perform a GET response', () => {
        
    })

})